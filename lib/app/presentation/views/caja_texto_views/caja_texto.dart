import 'package:flutter/material.dart';
class CajaTextoView extends StatelessWidget {
  const CajaTextoView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
          child: Text('CAJA_TEXTO_VIEW'),
        )
    );
  }
}