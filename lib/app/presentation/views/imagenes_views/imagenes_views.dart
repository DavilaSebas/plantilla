import 'package:flutter/material.dart';
class ImagenesView extends StatelessWidget {
  const ImagenesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
          child: Text('IMAGENES_VIEW'),
        )
    );
  }
}